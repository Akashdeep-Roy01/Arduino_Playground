% closext=zeros(10,7);
% for i=1:10
%     disp('CLOSE')
%     close=plotter(a);
%     closefeat=featurext(close);
%     closeext(i,:)=closefeat;
% end

% precext=zeros(10,7);
% for i=1:10
%    disp('PRECISION')
%    prec=plotter(a);
%    precfeat=featurext(prec);
%    precext(i,:)=precfeat;
% end

 opext=zeros(10,7);
for i=1:10
disp('OPEN')
open=plotter(a);
openfeat=featurext(open);
opext(i,:)=openfeat;
 end

        